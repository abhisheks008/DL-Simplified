# Project Structure Validator

This script validates that all project folders follow the repository's structure guidelines.

## Usage

```bash
python validate_structure.py
```

## What it checks

- Required directories: `Dataset/`, `Model/`, `Images/`
- Required files: `README.md`, `requirements.txt`
- Naming conventions (correct capitalization)
